%div.pw_reset_form_wrapper
  %h1 Reset your password

  = form_tag password_resets_path, method: :post do
    .field
      = label_tag :email
      = text_field_tag :email, params[:email], class: 'gtb_text_field'
    .actions
      = submit_tag 'Reset password', class: 'signup_button'
